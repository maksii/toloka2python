# Copy to .env and fill in. .env is gitignored.
# Used only for local runs (e.g. scripts or debug); not required if you set env vars in the shell or in your IDE launch config.

TOLOKA_USER=
TOLOKA_PASSWORD=
